<form class="delete-form" method="post" action="{{ path('vault_delete', {'id': vault.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vault.id) }}">
    <button class="btn">Supprimer</button>
</form>

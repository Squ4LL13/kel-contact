<form class="delete-form" method="post" action="{{ path('contact_delete', {'id': contact.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contact.id) }}">
    <button class="delete-btn">Supprimer le contact</button>
</form>

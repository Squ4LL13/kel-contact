{% extends 'base.html.twig' %}

{% block title %}Meet{% endblock %}

{% block body %}
    <h1>Meet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ meet.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ meet.title }}</td>
            </tr>
            <tr>
                <th>MeetingStart</th>
                <td>{{ meet.meetingStart ? meet.meetingStart|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>MeetingEnd</th>
                <td>{{ meet.meetingEnd ? meet.meetingEnd|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('calendar') }}">back to list</a>

    <a href="{{ path('meet_edit', {'id': meet.id}) }}">edit</a>

    {{ include('meet/_delete_form.html.twig') }}
{% endblock %}
